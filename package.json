{
  "name": "settimes",
  "version": "1.0.0",
  "type": "module",
  "description": "SetTimes.ca - Event Schedule & Performance Management Platform",
  "scripts": {
    "migrate:local": "bash scripts/migrate-local-db.sh",
    "validate:schema": "node scripts/validate-db-schema.js",
    "deploy:dev": "npm --prefix frontend run build && npx wrangler pages deploy frontend/dist --project-name settimesdotca --branch dev",
    "deploy:prod": "npm --prefix frontend run build && npx wrangler pages deploy frontend/dist --project-name settimesdotca --branch main",
    "prebuild": "npm run validate:schema",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.0",
    "@playwright/test": "^1.59.1",
    "@vitest/coverage-v8": "^4.1.4",
    "baseline-browser-mapping": "^2.10.18",
    "better-sqlite3": "^12.9.0",
    "vite": "^8.0.8",
    "vitest": "^4.1.4"
  },
  "dependencies": {
    "@lucia-auth/adapter-sqlite": "^3.0.2",
    "csrf-csrf": "^4.0.3",
    "email-validator": "^2.0.4",
    "lucia": "^3.2.2",
    "otplib": "^13.4.0"
  }
}
